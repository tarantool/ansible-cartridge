---
all:
  vars:
    cartridge_package_path: ./myapp-1.0.0-0.rpm
    cartridge_cluster_cookie: new-cookie

    cartridge_defaults:
      log_level: 4

  children:
    cluster:
      children:
        instances:
          children:
            host1:
              vars:
                ansible_host: vm1
                ansible_user: root
                ansible_connection: docker
                become: true
                become_user: root

              hosts:
                instance-1:
                  config:
                    advertise_uri: 'vm1:3311'
                    http_port: 8181

            host2:
              vars:
                ansible_host: vm2
                ansible_user: root
                ansible_connection: docker
                become: true
                become_user: root

              hosts:
                instance-2:
                  config:
                    advertise_uri: 'vm2:3312'
                    http_port: 8182

                instance-3:
                  config:
                    advertise_uri: 'vm2:3313'
                    http_port: 8183
