---

- name: 'Update instance code'
  file:
    src: '{{ instance_info.app_dir}}'
    dest: '{{ instance_info.instance_dir }}'
    owner: tarantool
    group: tarantool
    state: link
  when: cartridge_multiversion
  tags: cartridge-instances
